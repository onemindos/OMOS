# Disabled for OneMind OS fork — upstream workflow not needed
name: "DISABLED"
on: workflow_dispatch
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the OneMind OS fork"
