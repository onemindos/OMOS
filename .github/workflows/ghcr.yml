# Disabled for OneMind OS fork — replaced by build-omos.yml
# build-omos.yml builds ghcr.io/onemindos/omos:latest on every push to main
# When we need pmtiles/events/retention images, we add them to build-omos.yml
name: "DISABLED — GHCR (replaced by build-omos.yml)"
on:
  workflow_dispatch:
    # Disabled — no automatic triggers
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the OneMind OS fork. Use build-omos.yml instead."
